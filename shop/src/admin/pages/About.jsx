// About this app and Cloudgate: the shared page (same file in every App Store app) on a route.
import { CloudgateAbout } from '@/shared/CloudgateAbout';

const About = () => <CloudgateAbout />;

export { About };
